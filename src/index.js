import React from "react";
import ReactDOM from "react-dom";

import App from "./App";

// this line below directs DOM to render the app file and the render it in the index html file the div with the root ID
ReactDOM.render(<App />, document.getElementById("root"));
